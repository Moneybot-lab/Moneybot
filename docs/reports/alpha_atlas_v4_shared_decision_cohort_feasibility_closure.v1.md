# Alpha Atlas V4 saved-input shared-decision feasibility closure

## Closed status

**`COMPLETE — NOT_SUPPORTED_BY_SAVED_EVIDENCE`**

The metadata-only review completed in saved-evidence run
[`36065114141-1`](https://github.com/Moneybot-lab/Moneybot/actions/runs/36065114141/attempts/1).
At the unchanged proposed boundary—the official close of the XNYS session
immediately preceding `entry_at`—the saved records did not establish assembled
snapshot availability and freshness.

All **6,059** fold/partition opportunity assignments reconciled to
`UNKNOWN_AVAILABILITY_OR_FRESHNESS`; verified qualifying assignments were zero.
All three folds remained represented. Zero verified qualifying opportunities is
an evidence statement, not an absence statement: it does **not** mean that no
snapshots, securities, or trading opportunities existed. Source-bar timestamps
were not promoted into proof of assembled-snapshot availability, unknowns were
not made eligible, the boundary was not shifted, and no singleton cohorts were
forced.

## Registered regression retained status

The registered group-return regression remains **`NOT_EVALUABLE —
INCOMPATIBLE_SAVED_TIMING_EVIDENCE`**. Its blocked attempt produced **zero fits,
zero predictions, and zero performance evaluations**. This is not an
unfavorable model result because no model was fitted or evaluated. The separate
prior scoring run `35923707545-1` remains **`COMPLETE — UNFAVORABLE FINDINGS`**,
unchanged.

The original timing investigation remains `COMPLETE`. The exact-timestamp
amendment remains `NOT_APPROVED`; shared-boundary training remains
`NOT_AUTHORIZED / NOT_EXECUTED`. Historical coverage, terminal valuation,
net-return validation, and promotion gates remain unchanged and open.

## Evidence and verification boundary

The hosted report records that exact source retrieval and input verification
succeeded during run `36065114141-1`. Its workflow output artifact name is
`v4-shared-decision-cohort-feasibility-36065114141-1`. The hosted provenance,
compact JSON/Markdown, metadata-only mappings, and `SHA256SUMS` remain the
original evidence; they were not regenerated or modified for this closure.

Those hosted output bytes are not in this checkout, and this environment has no
authenticated GitHub session. Consequently the artifact ID, artifact digest,
current retention, hosted report hashes, and hosted checksum result are recorded
as unknown/not newly verified—not inferred from the run URL. Successful
retrieval during the historical run does not prove ongoing remote availability.
No large mapping, archive, or original training input was downloaded.

The locally available, previously committed pre-run compact records were newly
verified against their existing manifest:

| File | Verified SHA-256 |
|---|---|
| `alpha_atlas_v4_shared_decision_cohort_feasibility.json` | `2c808015fd89997792b682a38135b7dedadb70a4459bd9c9e8689d3d5921d94e` |
| `alpha_atlas_v4_shared_decision_cohort_feasibility.md` | `4edfd9eaa6ca7c02c9091e9ca588c5831ad64b440a8131b4d92255743deae408` |
| `alpha_atlas_v4_shared_decision_cohort_feasibility.provenance.json` | `31b072f8068d83ae82cb22054af3edd049025c199cb2c1c9b580e79ed4fd846b` |

These verified bytes predate hosted completion and are not represented as the
unavailable run `36065114141-1` output. The run's saved findings supply the
6,059-assignment result and completed classification; this supplemental record
only closes and scopes that result.

## Closed scope

No analyzer, timing investigation, or experiment was rerun. No training,
scoring, tuning, holdout-content access, provider request, production change,
prospective collection, or replacement experiment occurred. Large mappings and
archives remain outside Git.
