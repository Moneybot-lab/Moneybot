# Alpha Atlas V3.1 frozen benchmark

**Schema:** `alpha-atlas-v31-frozen-benchmark.v1`  
**Status:** benchmark-only; recovery workflow required

Repository code identifies candidate version `candidate-alpha-atlas-v31-clean-v1`, feature contract `moneybot-serving-features.v1`, and deterministic feature-list hash `7638eed893fdecc7bfc978e8b827871907d826692f344fc517a354defb530703`.

The exact historical evidence targets are Track B run #156 (`32700956267`, artifact `9510663000`) and #157 (`32820019463`, artifact `9553009280`). Their known GitHub artifact digests are recorded in the JSON report. This checkout has no authenticated GitHub CLI session, so extracted model/metadata hashes remain `RECOVERY_WORKFLOW_REQUIRED`, not reconstructed from memory. The manual **Recover V3.1 Benchmark Evidence** workflow downloads only those runs and emits compact hashes and comparison evidence.

V3.1 is benchmark-only. It cannot automatically promote or route live, is not V4 training evidence, and is not evidence that Alpha Atlas is ready for paid recommendations. No V3.1 model was retrained or altered.
