# Alpha Atlas V4 identity dependency and comparison-scope decision

## Evidence boundary

This supplement records the supplied summary of registration run `35741072083-1`
(internal hash `9c573b0c75b97dbbe9a1b3b17c694425fd647e6fcbd600d2fd2c636d51203608`).
The artifact was not locally re-downloaded because authenticated GitHub access is
unavailable. Artifact-bound unique-key, dependency-window, supported/conflicting,
and canonical-ID details remain pending the existing registration workflow.

## Established scope

- Split integrity is verified.
- All 24,846 development IDs use the ticker/date fallback pattern; this proves an
  observation key, not same-security continuity.
- All 10,273 validation returns match the traced gross split-adjusted formula.
- Dividend treatment is not established, applicable development costs are absent,
  and portfolio total return/drawdown remains unavailable.
- Zero direct historical matches does not resolve `PARTIAL_UNKNOWN` alias coverage.

## Proposed decision — review required

Recommend **Option A**, solely as a narrow frozen-sample diagnostic using the exact
registered candidate set and three OOF folds: probability calibration where score
semantics permit it, classification/decision coverage, and gross split-adjusted
endpoint-price diagnostics. Keep the registered rule unchanged. This cannot support
claims of net profitability, portfolio performance, durable predictive advantage,
promotion readiness, dividend-inclusive economics, or security continuity.

Option B remains inactive. A future net comparison would require a new policy,
registered before results, specifying components, units, one-way/round-trip
application, spread/slippage, and identical candidate/baseline treatment. It may not
borrow the final-holdout policy without separate justification and review.

**Approval boundary:** review this proposal and the artifact-bound identity report;
do not execute performance scoring from this document.
